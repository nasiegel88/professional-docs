# Set library path
.libPaths(c("/srv/conda/envs/notebook/lib/R/library" , .libPaths() ) )

# Install TinyTex
if (!tinytex::is_tinytex()) tinytex::install_tinytex(bundle = 'TinyTeX-2',
                                                     force = TRUE)

dir.create('bin', showWarnings = FALSE)
